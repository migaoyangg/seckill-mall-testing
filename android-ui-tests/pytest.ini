[pytest]
testpaths = tests
python_files = test_*.py
python_functions = test_*
addopts = -ra --strict-markers
markers =
    smoke: core flow suitable for every build
    regression: broader UI regression coverage
    mutating: creates or changes backend data

