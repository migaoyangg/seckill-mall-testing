# [姓名]

**求职意向：测试开发工程师 / 软件测试工程师（应届生/实习生）**  
[手机号] ｜ [邮箱] ｜ [所在城市] ｜ [GitHub/Gitee 地址]

## 教育经历

**[学校名称]｜[专业名称]｜[本科/专科]**　　　　　　　　　　　　　　[入学年月] - [毕业年月]

- 主修课程：[数据结构、计算机网络、操作系统、数据库原理、软件工程等，请按实际填写]
- 成绩/证书：[排名、奖学金、英语及其他证书，没有突出内容可删除本行]

## 专业技能

- 熟悉 Java、Python 和 SQL，能够阅读 Spring Boot 业务代码并围绕接口、业务规则、异常分支和数据状态开展测试。
- 熟悉 JUnit 5、Mockito、MockMvc、pytest、requests，掌握 fixture、参数化、Mock、接口关联及测试数据隔离。
- 熟悉 HTTP、RESTful API、JSON 和 JWT 鉴权，能够使用 curl/Postman 完成接口调试与测试。
- 熟悉 MySQL、Redis 基础操作，能够使用 SQL 和 Redis Key 校验订单、库存、幂等及数据一致性。
- 掌握 JMeter/wrk 性能测试基本方法，了解并发用户、吞吐量、平均响应时间、P95/P99 和错误率等指标。
- 熟悉 Git、Maven、Linux 常用命令；了解 RabbitMQ、Redis Lua、Redisson、Allure 和 CI/CD。
- 掌握 Appium、ADB、pytest 和 Page Object 移动端自动化方法，能够进行元素定位、显式等待、失败截图与日志采集。[完成框架后保留]

## 项目经历

### 秒购商城服务端测试自动化框架　　　　　　　　　　　　　　　　　　2026.07 - 至今

**项目技术：** Spring Boot 3、MySQL、Redis、RabbitMQ、JWT、JUnit 5、Mockito、MockMvc、pytest、requests、JMeter

**项目简介：** 面向包含用户、商品、普通订单、支付退款和限时抢购业务的电商交易系统，建设覆盖单元、Web、接口、数据一致性和性能场景的后端测试自动化框架。系统通过 Redis Lua 预扣库存、异步队列削峰、数据库乐观锁及唯一索引控制超卖与重复下单风险。

- 分析 Controller、Service、DTO 和订单状态机，梳理接口参数、权限矩阵、业务码及数据变化，设计正常、异常、边界、越权、幂等和状态流转测试。
- 使用 JUnit 5、Mockito 编写 Service 单元测试，覆盖登录校验、商品缓存、订单取消与库存回补、支付回调幂等、秒杀预扣及消息发送失败回滚等核心分支。
- 使用 MockMvc 验证 Controller 参数校验、JSON 响应、业务异常和管理员权限；定位参数类型错误返回 HTTP 500、商品管理路径漏拦截问题，修复后补充自动化回归。
- 基于 pytest + requests 搭建接口自动化框架，通过 fixture 管理多角色账号、Token 和 Session，使用参数化复用校验逻辑，并解决 Redis Token 覆盖导致的用例不稳定问题。
- 覆盖订单创建、查询、取消和支付链路，验证相同支付流水重复回调的幂等性及不同流水号的异常拦截；通过 SQL/Redis 校验订单状态和库存变化。
- 使用 JMeter/wrk 构建秒杀入口、异步结果轮询和订单链路压测场景，通过 SQL 校验订单数、重复下单及 Redis/MySQL 库存一致性；在 [并发数] 并发下 P95 为 [数值] ms、技术错误率为 [数值]%。[实测后替换]
- 建立自动化回归流水线，执行 Maven 与 pytest 测试并归档 Allure 报告；最终 Java 测试 [数量] 条、接口自动化 [数量] 条全部通过。[完成并复跑后替换]

### 秒购商城 Android 客户端自动化测试框架　　　　　　　　　　　　　2026.09 - 至今

**项目技术：** Python、pytest、Appium、UiAutomator2、ADB、Page Object、Allure、GitHub Actions｜被测端：Kotlin、Retrofit

**项目简介：** 以秒购商城 Android 客户端为被测对象，建设覆盖登录、商品浏览、下单和订单查询的端到端 UI 自动化框架，通过页面对象封装、测试数据隔离和失败现场采集提高移动端回归的可维护性与可追溯性。

- 分析客户端页面、控件及接口调用链，设计登录、商品列表、商品详情、创建订单、订单查询和退出登录等主流程及异常场景。 
- 基于 Appium + pytest 搭建自动化执行框架，设计 BasePage、LoginPage、HomePage、ProductDialog 4 个 Page Object，分离元素定位、页面操作和业务断言。
- 使用 pytest fixture 管理 Driver、登录前置与测试数据；下单前记录订单集合，UI 创建订单并验证后通过业务 API 自动取消，恢复库存且不遗留待支付订单。
- 建立失败现场采集机制，用例失败时自动保存截图、页面 XML 和 Logcat，并生成 pytest-html 与 Allure 结果。
- 使用 smoke、regression、mutating 标签分组执行，覆盖登录校验、状态恢复、商品详情、页面切换、订单查询与创建订单等 13 条用例，全量回归全部通过，耗时 202.47 秒。
- 编写 GitHub Actions Android CI，自动启动后端依赖与 Emulator、构建 APK、启动 Appium、执行 smoke 用例并归档测试证据。

## 自我评价

- 具备代码阅读、测试设计和缺陷定位能力，能够结合请求响应、应用日志、数据库和 Redis 状态建立问题证据链。
- 重视测试数据隔离、结果可复现和自动化用例可维护性，能够从功能、权限、幂等、状态流转和并发一致性等角度分析风险。
- 对测试开发方向有持续学习和实践意愿，能够快速熟悉业务并将重复测试过程沉淀为自动化能力。

---

## 内部使用说明（正式投递前删除）

1. 方括号内容必须替换为真实信息或真实执行结果，不能直接投递。
2. Android 项目中标有“完成后保留”的内容，目前仓库尚无对应代码；完成 Appium 最小闭环后再保留。
3. 性能结果必须来自实际 JMeter/wrk 执行，当前 `.jtl` 文件没有可用结果，不能填写示例数字。
4. 最后一次历史报告显示 34 个独立 Service/Controller 测试通过，另有 1 个集成测试通过；静态代码共 35 个 `@Test`。正式投递前重新执行并采用终端实际数字。
5. “平台”暂不写入项目名；具有用例管理、任务调度、设备管理、执行记录和报告中心后，再升级为测试平台。
